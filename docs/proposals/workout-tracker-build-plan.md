# Workout Tracker — build plan (proposal)

The **canonical** copy of this proposal lives in the **workout-tracker** application repository:

**[`workout-tracker/docs/proposals/workout-tracker-build-plan.md`](../workout-tracker/docs/proposals/workout-tracker-build-plan.md)**

Use that file for edits, PRs, and team review. This pointer exists so the plan appears in the **bible-support** `docs/proposals/` index when both trees are present in one workspace (e.g. devcontainer with a nested or sibling `workout-tracker` checkout).

If you maintain **only** a standalone `workout-tracker` clone, you do not need this pointer file.

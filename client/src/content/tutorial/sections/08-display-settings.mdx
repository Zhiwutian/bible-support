import { TutorialProse } from '@/components/tutorial/TutorialProse';

<TutorialProse>

## Display settings

Open the **menu** (top right), then **Display settings** to adjust:

- **Dark mode** — darker surfaces across the app. **Reading colors** (below)
  still control Light, Sepia, or Dark for Reader, Support, and Saved verses.
- **High contrast** — stronger contrast for controls and general UI text.
- **Reading colors** — Light, Sepia, or Dark for Bible Reader, Support
  scriptures, and Saved verse blocks (same storage as **Reader → Options →
  Theme**). Use **More reader settings** for font size, line spacing, and
  other reader-only options.
- **Text size** — larger or smaller type across menus and most pages (this is
  separate from Reader font size in Options).

These choices apply to your **device/browser** (guest or signed in) the way
other app settings do. Tap the same menu entry again to change or preview
settings.

</TutorialProse>
